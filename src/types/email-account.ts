import type {
  EmailAccount,
  EmailAccountInsert,
  EmailAccountUpdate,
  ProviderType,
  SyncStatus,
} from './database';

export type { EmailAccount, EmailAccountInsert, EmailAccountUpdate, ProviderType, SyncStatus };
