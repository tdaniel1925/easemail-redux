# Provider Adapter Pattern — EaseMail v2
## File Structure: lib/providers/ (types.ts, index.ts, microsoft.ts, google.ts, normalize.ts, token-manager.ts)
## Token Manager: SELECT FOR UPDATE row lock, proactive refresh 5min before expiry
## Folder Mapping: Graph wellKnownName and Gmail labels mapped to standard folder_type enum
